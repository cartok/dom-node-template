* UNSUPPORT application/xml!!!

Possible NodeTemplate text input:

nr: 1
description: any svg tag 
example: <g>...</g>
MUST BE USED AS: new NodeTemplate("...", "svg")
todo: DONE

nr: 2
description: svg tag 
example: <svg></svg>
CAN BE USED AS: new NodeTemplate("...")
todo: DONE

nr: 3
description: html tag 
example: <div></div>
todo: DONE

nr: 4
description: html tag (tags) with svg inside 
example: <div><svg></svg></div>
todo: DONE


PARSE EVERYTHING AS text/html per default if svg has namespace attribute?





* HOW TO COMBINE image/svg+xml fragment with DocumentFragment?
> only matters if (type === "image/svg+xml") and (firstTag === svg)
  wenn das erste tag in dem fall bspw. <g></g> ist 
  müsste es so bleiben können und darf nur an nodes
  mit ownerDocument = svg angehängt werden...! 
    -_-----_>>_>_>__>_>_> (test notwendig)
  
  if svg fragment: x.documentElement

xmlns="http://www.w3.org/1999/xhtml"


@thesis: cloneNode
Der duplizierte Knoten enthält alle Attribute und deren Werte, aber nicht die Eventlistener (https://developer.mozilla.org/de/docs/Web/API/Node/cloneNode)
Der mit cloneNode() duplizierte Knoten ist noch nicht Teil des Dokuments und hat keinen Elternknoten bis er mithilfe der Methode Node.appendChild() oder einer anderen vergleichbaren Methode in das Dokument eingefügt wird.
Um einen Knoten aus einem anderen Dokument zu kopieren sollte die Document.importNode() verwendet werden.



* HOW TO EXPOSE the parsed result under this.fragment?!?!
> 


****************






*   if type=image/svg+xml
    add namespaceURI: http://www.w3.org/2000/svg (to first svg tag)
*   if type=text/html && if text has <svg .*> 
    add namespaceURI: http://www.w3.org/2000/svg
    or for every svg add namespaceURI