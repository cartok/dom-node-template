{
  "name": "dom-node-template",
  "version": "2.1.0",
  "description": "create usefull html or svg element wrapper object with es6 template string.",
  "keywords": [
    "dom",
    "node",
    "template",
    "jsx",
    "dom parser",
    "template string",
    "string template",
    "template literal"
  ],
  "main": "./build/NodeTemplate.js",
  "scripts": {
    "sound:plock":      "play -q -v 0.2 $(find /usr/share/sounds/ -type f -name *device-added* | head -n 1)",
    "sound:microwave":  "play -q -v 0.1 $(find /usr/share/sounds/ -type f -name *complete* | head -n 1)",
    "sound:trash":      "play -q -v 0.15 $(find /usr/share/sounds/ -type f -name *trash*  | head -n 1)",
    "clean:js":         "rm $(find ./build/ -type f -regex '.*\\.[jt]sx?') && npm run sound:trash",
    "clean:dir":        "rm -rf ./build && mkdir build && npm run sound:trash",
    "babelrc:build":    "cp ./.babelrc.build ./.babelrc",
    "babelrc:mocha":    "cp ./.babelrc.mocha ./.babelrc",
    "babel":            "babel --out-dir ./build ./src",
    "build":            "clear; npm install && npm run clean:dir && npm run babelrc:build && npm run babel && npm run sound:microwave",
    "build:server":     "npm install && npm run clean:dir && npm run babelrc:build && npm run babel",
    "test":             "clear; npm run clean:dir && npm run babelrc:mocha && npm run babel && mocha --require babel-core/register --require jsdom-global/register --",
    "test:inspect":     "clear; npm run clean:dir && npm run babelrc:mocha && npm run babel && mocha --require babel-core/register --require jsdom-global/register --inspect-brk --watch --",
    "test:monitor":     "nodemon -x \"npm run test\"",
    "test:all":         "clear; npm run clean:dir && npm run babelrc:mocha && npm run babel && mocha --require babel-core/register --require jsdom-global/register && npm run sound:plock",
    "test:all:monitor": "nodemon -x \"npm run test:all\"",
    "prepublishOnly":   "npm run test:all && npm run  build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cartok/dom-node-template"
  },
  "author": "Dennis Weiershäuser <dennis.weiershaeuser@l3p-cv.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "*",
    "babel-eslint": "*",
    "babel-plugin-transform-flow-strip-types": "*",
    "babel-preset-env": "*",
    "chai": "*",
    "eslint": "*",
    "eslint-plugin-flowtype": "*",
    "flow-bin": "*",
    "http-server": "^0.10.0",
    "jquery": "3.x.x",
    "jsdom": "*",
    "jsdom-global": "*",
    "mocha": "*",
    "nodemon": "*"
  },
  "dependencies": {
    "html-tag-names": "*",
    "svg-tag-names": "*"
  },
  "nodemonConfig": {
    "ignore": [
      "build",
      "built",
      "dist",
      "bin",
      "node_modules"
    ]
  }
}
